extends ../layout

block variables
    - var page_title = "Admin"

block content
    form#editForm(method="get" action="#")
        input(type="hidden", name="_id" value=_id)
        p
            label(for="title")
            input#title(type="text", name="title", value=title)
        p
            label(for="author")
            input#author(type="text", name="author", value=author)
        p
            label(for="pub_date")
            input#pub_date(type="date", name="pub_date", value=pub_date)
        p
            label(for="content")
            textarea#content(name="content", cols="30", rows="10")= content
        button#deleteBtn Delete
        button(type="submit") Save Post